
<!-- Change friendships model to have user_id, friend_id, message, answer, role_link, portfolio_link

when user clicks friendship, render friendship_new:
render form with:
- user_id
- friend_id
- blank message field
- blank role_link field
- blank portfolio_link field
- friend.profile.criteria_question
- blank answer field -->


<section>
  <div class="container">
    <div class="row">
  <!-- <div class="col-xs-2 float-left"> -->
    <div class='col-xs-3 float-left'>
      <%= form_for @friendship do |f| %>
            <%= link_to profile_path(@friendship.friend.profile) do %>
            <%= image_tag @friendship.friend.profile.image %>
            <center>
            <p><%= @friendship.friend.profile.firstname %> <%= @friendship.friend.profile.lastname %></p> <%= @friendship.friend.profile.role%> at <%= @friendship.friend.profile.company %>
            <center>
            <% end %>
      <% end %>
    </div>
  <!-- </div> -->
  <div class="col-xs-9 float-left">
    <h3>Referral Interview Request</h3>
    <div class= "well clearfix">
      <%= form_for @friendship do |f| %>

        <br>
        <div>
          <label>Message:</label>
          <%= f.text_area :message, rows: 10, class:"form-control", placeholder: "Write your message here" %>

          <label>Job Link:</label>
          <%= f.text_field :link_role, class:"form-control", placeholder: "Link to Desired Role" %>

          <label>Personal Website:</label>
          <%= f.text_field :link_portfolio, class:"form-control", placeholder: "Link to Portfolio" %>

          <label> Interview Question: </label>
          <%= f.text_field :link_portfolio, class:"form-control", placeholder: " Sourcer Criteria Question" %>
          <%=@friendship.friend.profile.criteria_question %>

          <label> Interview Response: </label>
          <%= f.text_area :message, rows: 10, class:"form-control", placeholder: "Write your message here" %>



        </div>
        <!-- <label>Role</label>
          <%=f.hidden_field :user_id, :value => current_user.id %>
          <%=f.hidden_field :author, :value => current_user.profile.full_name %>
        <div> -->

        </div>
        <br>

        <%= f.submit "Submit", class:"btn btn-primary btn-lg pull-right" %>
        <% end %>
      </div>
    </div>
  </div>


  </div><!--  end row -->
  </div>
</section>